import React from 'react';
import { View, Text } from 'react-native';
import { navigate } from '../../navigationRef';

const addCommentScreen = ({ navigation }) => {

	return (
		<View>
			<Text> Pantalla de mis Posts </Text>
		</View>
	);
};

export default (addCommentScreen);
